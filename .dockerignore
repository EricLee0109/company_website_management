node_modules
.git
.gitignore
.env*
# Payload media must mount as volume, not bake into image
media
# Payload uploads must mount as volume, not bake into image
uploads
# Log files
*.log
# Docker files
Dockerfile*
docker-compose*
# Cursor markdown files
.cursor
# Next.js files
.next
# Payload files
.payload
# Build output
dist
